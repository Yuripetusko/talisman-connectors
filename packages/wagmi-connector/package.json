{
  "name": "@talismn/wagmi-connector",
  "version": "0.0.3",
  "license": "GPL-3.0-or-later",
  "author": "TalismanSociety",
  "publishConfig": {
    "access": "public"
  },
  "main": "dist/index.js",
  "module": "dist/wagmi-connector.esm.js",
  "typings": "dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "keywords": [
    "Talisman",
    "wagmi",
    "connector"
  ],
  "scripts": {
    "dev": "tsdx watch --target node --verbose",
    "build": "tsdx build --target node",
    "lint": "tsdx lint src",
    "prepublishOnly": "rimraf dist && tsdx build --target node",
    "clean": "rimraf dist && rimraf node_modules"
  },
  "peerDependencies": {
    "@wagmi/core": "^0.4.7"
  },
  "devDependencies": {
    "@wagmi/core": "^0.4.7",
    "ethers": "^5.6.9",
    "typescript": "^4.7.4"
  }
}
